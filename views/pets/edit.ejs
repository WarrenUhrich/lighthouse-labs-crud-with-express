<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Pet</title>
</head>
<body>
    <h1>Edit Pet</h1>

    <!-- POST /pets/:key    -->

    <form method="POST" action="/pets/<%= pet.name %>">
        <label>
            Name:
            <input name="name" value="<%= pet.name %>">
        </label>
        <label>
            Age:
            <input name="age" value="<%= pet.age %>">
        </label>
        <label>
            Type:
            <input name="type" value="<%= pet.type %>">
        </label>

        <input type="submit" value="Update Pet">
    </form>

    <form method="POST" action="/pets/<%= pet.name %>/delete">
        <input type="submit" value="Delete Pet">
    </form>

</body>
</html>